<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="supportApply">
	<select id="applyList" parameterType="map" resultType="supportApply">
 		select  
 			support_apply_no as supportApplyNo,
 			support_apply_reason as supportApplyReason,
 			support_apply_state as supportApplyState,
 			support_no as supportNo,
 			support_apply_id as supportApplyId
 			
 			from supportApply
			where support_apply_id = #{supportApplyId }	
 	 </select>

  	<insert id="insertApply" parameterType="supportApply">
  		insert into supportapply values(
  		1,#{supportApplyReason},0,#{supportNo},#{supportApplyId}
  		)
  		
  	</insert>
</mapper>
